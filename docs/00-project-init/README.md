# 00 — Project Init

## Checklist
- [ ] Tạo repo GitHub
- [ ] Init frontend trong `source/frontend/`
- [ ] Init backend trong `source/backend/` (nếu cần)
- [ ] Setup Vercel + connect GitHub

## Commands — Frontend
```bash
cd source/frontend
npm create vite@latest . -- --template react-ts
npm install framer-motion gsap react-router-dom
npm install -D tailwindcss@4 @tailwindcss/postcss autoprefixer
```
