# 04 — Backend

## Status
Not needed for initial launch. Portfolio is static (frontend only).

## If extended later
- Platform: Supabase (DB + Auth + Storage)
- Use case: admin panel, dynamic projects, blog

## Source code location
`source/backend/`

## Schema (future)
- `projects` — id, title, description, image, tech[], demo_url, github_url
- `stats` — id, value, label
- `portfolio_settings` — hero_title, hero_video_url, email
