# 01 — System Design

## Architecture
SPA frontend (React + Vite). Optional backend via Supabase.

## Sections
Hero → Stats → Selected Works → Contact

## Design tokens
Defined in `source/frontend/src/index.css` under `@theme`.

## Handoff notes for agents
- Frontend builds in `source/frontend/src/components/`
- Backend (if needed) lives in `source/backend/`
- Content output at `docs/02-content/content-output.json`
